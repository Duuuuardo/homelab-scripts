# Proxy hosts

Ver documentação principal:

```txt
docs/nginx-proxy-manager.md
```

Resumo:

Todos os domínios `.lab` apontam no AdGuard para:

```txt
192.168.0.20
```

O Nginx Proxy Manager encaminha cada domínio para o IP e porta do serviço.
